---
name: yoshkar-ola-open-data
description: Use this skill when working with open datasets for the urban district "Город Йошкар-Ола", including currently available schools and kindergartens, and future public datasets exposed through the Yoshkar-Ola Public MCP server.
version: "0.1.8"
---

# Открытые данные городского округа "Город Йошкар-Ола"

Используй этот skill, когда пользователь спрашивает о наборах открытых данных
городского округа "Город Йошкар-Ола".

Версия skill: `0.1.8`.

Доступные на первом этапе наборы:

- муниципальные школы;
- муниципальные детские сады.

В дальнейшем состав наборов может расширяться. Не ограничивай описание
возможностей только школами и детскими садами, если пользователь спрашивает о
проекте в целом.

## Источник данных

Основной эталонный источник данных - "Цифровой мозг городского округа".
Данные поддерживаются в актуальном состоянии за счет регулярных
автоматизированных обновлений из государственных информационных систем,
отраслевых информационных ресурсов и иных официальных источников.

## Правила работы

- В начале работы с данными городского округа вызови MCP-инструмент
  `get_server_info`. Если `skill_version` в ответе больше локальной версии
  skill, сообщи пользователю, что доступно обновление, и покажи команду из
  `update_commands.codex_skill`.
- Если MCP-клиент поддерживает resources или prompts, используй актуальные
  инструкции с сервера: `yoshkar-ola://guidance/open-data` или prompt
  `yoshkar_ola_open_data_guidance`.
- Если MCP-клиент поддерживает resources, получай актуальный список слоев из
  `yoshkar-ola://layers`.
- Если вопрос касается доступных наборов данных городского округа, сначала
  используй MCP-инструменты `yoshkar-ola-public-mcp`.
- Для новых или неизвестных заранее слоев предпочитай универсальные инструменты
  `layer_list`, `layer_schema`, `layer_query` и `layer_get`.
- Для выбора слоя по вопросу используй `layer_suggest`.
- Для подготовки ответа модели используй `layer_answer_context`: он возвращает
  факты, источники и правила ответа. Не отвечай сверх этих фактов.
- Для аналитики и контроля качества используй `layer_stats`, `layer_facets`,
  `quality_summary` и `quality_findings`.
- Для диагностики подключения и состояния сервера используй `mcp_diagnostics`.
- Не отвечай из памяти, если нужные сведения можно получить через MCP.
- Не добавляй телефоны, адреса, сайты, ФИО, ИНН, лицензии и другие реквизиты,
  которых нет в ответе MCP.
- Если данных нет в открытом наборе, прямо скажи, что в доступном открытом
  наборе такие сведения не найдены.
- Если найдено несколько похожих организаций, покажи короткий список
  совпадений и уточни, какая запись нужна пользователю.
- Если пользователь сообщает о неточности, не исправляй данные самостоятельно.
  Скажи, что сведения нужно проверить в источнике и передать сопровождающим
  набора данных.

## Предпочтительный формат ответа

Для одной найденной организации:

- название;
- адрес;
- телефон;
- email;
- сайт;
- ИНН;
- руководитель, если поле доступно;
- сведения о лицензии, если они нужны пользователю.

Для списков:

- возвращай краткую таблицу или маркированный список;
- не перегружай ответ всеми реквизитами, если пользователь просит только найти
  организацию;
- предлагай уточнить запись, если результатов много.
